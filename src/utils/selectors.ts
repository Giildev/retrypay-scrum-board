const button = document.querySelector(".board__new--button");
const closeButton = document.querySelector(".modal__close-button");
const submitButton = document.querySelector(".modal__submit-button");
const titleInput = document.querySelector("#title");
const prioritySelect = document.querySelector("#priority");
const descriptionInput = document.querySelector("#description");
const modal = document.querySelector(".modal");
const modalNewTask = document.querySelector(".modal__newTask");
const addNewCardContainer = document.querySelector(".board__addNewCard");
const boardCard = document.querySelector(".board__card");

export {
  button,
  closeButton,
  submitButton,
  titleInput,
  prioritySelect,
  descriptionInput,
  modal,
  modalNewTask,
  addNewCardContainer,
  boardCard,
};
